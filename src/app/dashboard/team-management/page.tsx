import ClientTeamManagementPage from './ClientTeamManagementPage';

export default function TeamManagementPage() {
  // If you need to fetch data on the server, do it here and pass as props
  // For now, just render the client component
  return <ClientTeamManagementPage />;
}
