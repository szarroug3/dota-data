---
description: 
alwaysApply: true
---
- Handle errors gracefully in all code.
- Provide meaningful error messages for debugging and user feedback.
- Avoid silent failures or swallowing errors without logging or handling.

<example>
Agent adds try/catch blocks and logs errors with context.
</example>
<example type="invalid">
Agent ignores a rejected promise or leaves a catch block empty.
</example>
